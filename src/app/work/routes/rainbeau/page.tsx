import React from 'react';

import { ProjectContent, projectContent } from '@/constants/content';
type Props = {};

const rainbeauPage = (props: Props) => {
  const { project } = projectContent;

  return (
    <>
      <h2>{}</h2>
      <div></div>
    </>
  );
};

export default rainbeauPage;
